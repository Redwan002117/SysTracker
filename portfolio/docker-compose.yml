services:
  portfolio:
    build: .
    container_name: systracker-portfolio
    restart: unless-stopped
    ports:
      - "2222:80"
    environment:
      - NEXT_PUBLIC_DASHBOARD_URL=http://localhost:7777
      - NEXT_PUBLIC_GITHUB_REPO=Redwan002117/SysTracker
