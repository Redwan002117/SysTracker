# ============================================
# DEPRECATED: Use ../Dockerfile instead
# ============================================
# This Dockerfile has been moved to the repository root.
# The new Dockerfile includes:
# - Multi-stage build (dashboard + server)
# - Optimized caching
# - Security improvements (non-root user)
# - Health checks
# - Modern Next.js build process
#
# To build: docker build -t systracker:latest .
# ============================================

# See: ../Dockerfile for the current version

