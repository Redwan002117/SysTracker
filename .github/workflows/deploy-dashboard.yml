name: Deploy Dashboard to Production

on:
  push:
    branches:
      - main
    paths:
      - 'dashboard/**'
      - '.github/workflows/deploy-dashboard.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging
          - demo
      restart_server:
        description: 'Restart server after deployment'
        required: false
        type: boolean
        default: true

env:
  NODE_VERSION: '22'

jobs:
  build-and-deploy:
    name: Build & Deploy Dashboard
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'production' }}
    
    steps:
      - name: 🛎️ Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: dashboard/package-lock.json

      - name: 📥 Install Dependencies
        working-directory: dashboard
        run: |
          npm ci --prefer-offline --no-audit
          
      - name: 🔨 Build Dashboard
        working-directory: dashboard
        run: |
          npm run build
          
      - name: ✅ Verify Build
        working-directory: dashboard
        run: |
          if [ ! -d "out" ]; then
            echo "❌ Build failed - output directory not found"
            exit 1
          fi
          echo "✅ Build successful"
          echo "📊 Files: $(find out -type f | wc -l)"
          echo "📦 Size: $(du -sh out | cut -f1)"
          
      - name: 📊 Build Report
        working-directory: dashboard
        run: |
          echo "## 🚀 Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Files | $(find out -type f | wc -l) |" >> $GITHUB_STEP_SUMMARY
          echo "| Size | $(du -sh out | cut -f1) |" >> $GITHUB_STEP_SUMMARY
          echo "| Build Time | $(date -u +'%Y-%m-%d %H:%M:%S UTC') |" >> $GITHUB_STEP_SUMMARY
          echo "| Node Version | ${{ env.NODE_VERSION }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Environment | ${{ github.event.inputs.environment || 'production' }} |" >> $GITHUB_STEP_SUMMARY

      - name: 🌐 Install Cloudflare Tunnel Client
        if: github.event_name != 'pull_request'
        run: |
          curl -fsSL https://pkg.cloudflare.com/cloudflare-main.gpg | \
            sudo gpg --dearmor -o /usr/share/keyrings/cloudflare-main.gpg
          echo "deb [signed-by=/usr/share/keyrings/cloudflare-main.gpg] \
            https://pkg.cloudflare.com/cloudflared any main" | \
            sudo tee /etc/apt/sources.list.d/cloudflared.list
          sudo apt-get update -qq && sudo apt-get install -y cloudflared
          cloudflared version

      - name: 🔑 Setup SSH via Cloudflare Tunnel
        if: github.event_name != 'pull_request'
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
        run: |
          if [ -n "$SSH_PRIVATE_KEY" ]; then
            mkdir -p ~/.ssh
            echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
            chmod 600 ~/.ssh/id_ed25519
            {
              echo "Host ssh.monitor.rico.bd"
              echo "  ProxyCommand cloudflared access ssh --hostname %h"
              echo "  IdentityFile ~/.ssh/id_ed25519"
              echo "  StrictHostKeyChecking no"
              echo "  User ${REMOTE_USER}"
            } >> ~/.ssh/config
            chmod 600 ~/.ssh/config
            echo "✅ SSH + Cloudflare Tunnel configured"
          else
            echo "⚠️ SSH key not configured, deployment will be skipped"
          fi
          
      - name: 💾 Create Remote Backup
        if: github.event_name != 'pull_request'
        env:
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
          REMOTE_PATH: ${{ secrets.REMOTE_PATH }}
        run: |
          if [ -n "$REMOTE_USER" ] && [ -n "$REMOTE_PATH" ]; then
            BACKUP_NAME="dashboard-backup-$(date +%Y%m%d-%H%M%S)"
            ssh ${REMOTE_USER}@ssh.monitor.rico.bd \
              "cd ${REMOTE_PATH} && [ -d server/dashboard-dist ] && tar -czf backups/${BACKUP_NAME}.tar.gz server/dashboard-dist || mkdir -p backups"
            echo "✅ Backup created: ${BACKUP_NAME}.tar.gz"
          else
            echo "⚠️ Deployment not configured, skipping backup"
          fi

      - name: 🚀 Deploy to Production
        if: github.event_name != 'pull_request'
        env:
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
          REMOTE_DASHBOARD_PATH: ${{ secrets.REMOTE_DASHBOARD_PATH }}
        run: |
          if [ -n "$REMOTE_USER" ] && [ -n "$REMOTE_DASHBOARD_PATH" ]; then
            rsync -avz --delete \
              -e "ssh -F ~/.ssh/config" \
              --exclude 'node_modules' \
              --exclude '.next' \
              --exclude '.git' \
              dashboard/out/ \
              ${REMOTE_USER}@ssh.monitor.rico.bd:${REMOTE_DASHBOARD_PATH}/
            echo "✅ Dashboard deployed successfully"
          else
            echo "⚠️ Deployment not configured, skipping deployment (build artifacts available)"
          fi

      - name: 🔄 Restart Server
        if: github.event_name != 'pull_request' && (github.event.inputs.restart_server == 'true' || github.event_name == 'push')
        env:
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
          SERVER_SERVICE: ${{ secrets.SERVER_SERVICE || 'systracker' }}
          USE_PM2: ${{ secrets.USE_PM2 || 'false' }}
          PM2_APP_NAME: ${{ secrets.PM2_APP_NAME || 'systracker' }}
        run: |
          if [ -n "$REMOTE_USER" ]; then
            if [ "$USE_PM2" = "true" ]; then
              ssh ${REMOTE_USER}@ssh.monitor.rico.bd "pm2 restart ${PM2_APP_NAME}"
              echo "✅ Server restarted via PM2"
            else
              ssh ${REMOTE_USER}@ssh.monitor.rico.bd "docker restart ${SERVER_SERVICE}"
              echo "✅ Container restarted: ${SERVER_SERVICE}"
            fi
          else
            echo "⚠️ Remote server not configured, skipping restart"
          fi

      - name: ✅ Verify Deployment
        if: github.event_name != 'pull_request'
        env:
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
          REMOTE_DASHBOARD_PATH: ${{ secrets.REMOTE_DASHBOARD_PATH }}
        run: |
          if [ -n "$REMOTE_USER" ] && [ -n "$REMOTE_DASHBOARD_PATH" ]; then
            FILE_COUNT=$(ssh ${REMOTE_USER}@ssh.monitor.rico.bd \
              "find ${REMOTE_DASHBOARD_PATH} -type f | wc -l")
            DIR_SIZE=$(ssh ${REMOTE_USER}@ssh.monitor.rico.bd \
              "du -sh ${REMOTE_DASHBOARD_PATH} | cut -f1")
            
            echo "## 📊 Deployment Verification" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| Files Deployed | ${FILE_COUNT} |" >> $GITHUB_STEP_SUMMARY
            echo "| Directory Size | ${DIR_SIZE} |" >> $GITHUB_STEP_SUMMARY
            echo "| Server | ssh.monitor.rico.bd (Cloudflare Tunnel) |" >> $GITHUB_STEP_SUMMARY
            echo "| Deploy Time | $(date -u +'%Y-%m-%d %H:%M:%S UTC') |" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Remote server not configured, skipping verification"
          fi

      - name: � Notify Telegram
        if: always() && github.event_name != 'pull_request'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -z "$TELEGRAM_BOT_TOKEN" ] || [ -z "$TELEGRAM_CHAT_ID" ]; then
            echo "Telegram not configured, skipping notification"
            exit 0
          fi
          if [ "${{ job.status }}" = "success" ]; then
            ICON="✅"
            STATUS="succeeded"
          else
            ICON="❌"
            STATUS="failed"
          fi
          COMMIT_MSG=$(echo "${{ github.event.head_commit.message }}" | head -1)
          MESSAGE="${ICON} *SysTracker Deploy ${STATUS}*

🌿 Branch: \`${{ github.ref_name }}\`
👤 By: \`${{ github.actor }}\`
📝 \`${COMMIT_MSG}\`
🔗 [View Run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})"
          RESULT=$(curl -s -X POST \
            "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d parse_mode="Markdown" \
            -d disable_web_page_preview="true" \
            -d text="${MESSAGE}")
          if echo "$RESULT" | grep -q '"ok":true'; then
            echo "✅ Telegram notification sent"
          else
            echo "⚠️ Telegram error: $RESULT"
          fi

      - name: �📢 Notify Slack
        if: success() && github.event_name != 'pull_request'
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
        run: |
          if [ -n "$SLACK_WEBHOOK" ]; then
            curl -X POST $SLACK_WEBHOOK \
              -H 'Content-Type: application/json' \
              -d "{
                \"text\":\"🚀 SysTracker Dashboard deployed to ${{ github.event.inputs.environment || 'production' }}\",
                \"attachments\":[{
                  \"color\":\"good\",
                  \"fields\":[
                    {\"title\":\"Environment\",\"value\":\"${{ github.event.inputs.environment || 'production' }}\",\"short\":true},
                    {\"title\":\"Deployed By\",\"value\":\"${{ github.actor }}\",\"short\":true},
                    {\"title\":\"Commit\",\"value\":\"${{ github.sha }}\",\"short\":true},
                    {\"title\":\"Branch\",\"value\":\"${{ github.ref_name }}\",\"short\":true}
                  ]
                }]
              }"
          else
            echo "Slack webhook not configured, skipping notification"
          fi

      - name: 📢 Notify Discord
        if: success() && github.event_name != 'pull_request'
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          if [ -n "$DISCORD_WEBHOOK" ]; then
            curl -X POST $DISCORD_WEBHOOK \
              -H 'Content-Type: application/json' \
              -d "{
                \"content\":\"🚀 **SysTracker Dashboard Deployed**\",
                \"embeds\":[{
                  \"color\":5832650,
                  \"fields\":[
                    {\"name\":\"Environment\",\"value\":\"${{ github.event.inputs.environment || 'production' }}\",\"inline\":true},
                    {\"name\":\"Deployed By\",\"value\":\"${{ github.actor }}\",\"inline\":true},
                    {\"name\":\"Commit\",\"value\":\"${{ github.sha }}\",\"inline\":false}
                  ],
                  \"timestamp\":\"$(date -u +%Y-%m-%dT%H:%M:%S.000Z)\"
                }]
              }"
          else
            echo "Discord webhook not configured, skipping notification"
          fi

      - name: ❌ Rollback on Failure
        if: failure() && github.event_name != 'pull_request'
        env:
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
          REMOTE_PATH: ${{ secrets.REMOTE_PATH }}
        run: |
          if [ -n "$REMOTE_USER" ] && [ -n "$REMOTE_PATH" ]; then
            echo "⚠️ Deployment failed, attempting rollback..."
            LATEST_BACKUP=$(ssh ${REMOTE_USER}@ssh.monitor.rico.bd \
              "cd ${REMOTE_PATH}/backups && ls -t dashboard-backup-*.tar.gz | head -1")
            if [ -n "$LATEST_BACKUP" ]; then
              ssh ${REMOTE_USER}@ssh.monitor.rico.bd \
                "cd ${REMOTE_PATH} && tar -xzf backups/${LATEST_BACKUP}"
              echo "✅ Rolled back to: ${LATEST_BACKUP}"
            else
              echo "❌ No backup found for rollback"
            fi
          else
            echo "⚠️ Remote server not configured, skipping rollback"
          fi

  notify-failure:
    name: Notify on Failure
    runs-on: ubuntu-latest
    needs: build-and-deploy
    if: failure()
    steps:
      - name: � Telegram Failure Alert
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -n "$TELEGRAM_BOT_TOKEN" ] && [ -n "$TELEGRAM_CHAT_ID" ]; then
            curl -s -X POST \
              "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
              -d chat_id="${TELEGRAM_CHAT_ID}" \
              -d parse_mode="Markdown" \
              -d disable_web_page_preview="true" \
              -d text="🚨 *SysTracker Deploy FAILED*

👤 By: \`${{ github.actor }}\`
🔗 [View Failed Run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})"
            echo "✅ Telegram failure alert sent"
          else
            echo "Telegram not configured"
          fi

      - name: �📢 Send Failure Alert
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
        run: |
          if [ -n "$SLACK_WEBHOOK" ]; then
            curl -X POST $SLACK_WEBHOOK \
              -H 'Content-Type: application/json' \
              -d "{
                \"text\":\"⚠️ SysTracker Dashboard deployment FAILED\",
                \"attachments\":[{
                  \"color\":\"danger\",
                  \"fields\":[
                    {\"title\":\"Environment\",\"value\":\"${{ github.event.inputs.environment || 'production' }}\",\"short\":true},
                    {\"title\":\"Failed By\",\"value\":\"${{ github.actor }}\",\"short\":true},
                    {\"title\":\"Workflow\",\"value\":\"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\",\"short\":false}
                  ]
                }]
              }"
          else
            echo "Slack webhook not configured, skipping failure alert"
          fi
