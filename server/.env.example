PORT=3001
DATABASE_URL=postgres://postgres:password@localhost:5432/systracker
API_KEY=YOUR_STATIC_API_KEY_HERE

# JWT Configuration (Optional - auto-generated if not set)
# JWT_SECRET=your-very-long-random-secret-key
# JWT_EXPIRES_IN=24h

# Brevo SMTP Configuration for External Emails (Optional)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-brevo-smtp-user
SMTP_PASS=your-brevo-smtp-key
SMTP_FROM="SysTracker" <no-reply@yourdomain.com>

# Google OAuth Configuration (Optional)
# Get credentials from: https://console.cloud.google.com/apis/credentials
# 1. Create a new project or select existing
# 2. Enable Google+ API
# 3. Create OAuth 2.0 Client ID > Web application
# 4. Add authorized redirect URIs:
#    - http://localhost:3001/api/auth/google/callback (development)
#    - https://yourdomain.com/api/auth/google/callback (production)
# 5. Copy Client ID and Client Secret below
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3001/api/auth/google/callback
