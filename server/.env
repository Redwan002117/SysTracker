# Backend server port (reverse proxy forwards external traffic to this port)
PORT=3001
DATABASE_URL=postgres://postgres:password@localhost:5432/systracker
API_KEY=YOUR_STATIC_API_KEY_HERE

# Brevo SMTP Configuration for External Emails
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-brevo-smtp-user
SMTP_PASS=your-brevo-smtp-key
SMTP_FROM="SysTracker" <no-reply@yourdomain.com>

# Google OAuth Configuration (OPTIONAL - Leave blank to disable)
# ================================================================
# If configured: Users can sign in with Google (recommended for internet-facing deployments)
# If NOT configured: Users will only see username/password login (suitable for local/internal networks)
# 
# To enable Google OAuth:
# 1. Get credentials from: https://console.cloud.google.com/apis/credentials
# 2. Create OAuth 2.0 Client ID > Web application
# 3. Add authorized JavaScript origins:
#    - https://systracker.rico.bd (brand site)
#    - https://monitor.rico.bd (app server)
# 4. Add authorized redirect URI: https://monitor.rico.bd/api/auth/google/callback
# 5. Paste credentials below and restart server
#
# To disable Google OAuth (for local/internal use):
# - Leave these blank or comment them out
# - Only username/password authentication will be available
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=https://monitor.rico.bd/api/auth/google/callback
